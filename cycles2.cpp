#include <stdio.h>

	//вывод таблицы умножения

int main()
{
	for(int i=1;i<10;i++);  //фор используя счетчик i, равное 1, идет до значения меньше 10 и i на каждом шаге инкрементируется, т.е. увеличивается на 1
	{
		printf("%i\n", i);
	}
}
